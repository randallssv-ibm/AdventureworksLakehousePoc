# This is a Databricks asset bundle definition for silver_layer_common.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: silver_layer_common
  uuid: 7ea1a0d5-ceac-4850-b730-af9a068b0a03

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405607848397610.10.azuredatabricks.net
    variables:
      catalog: commonsilver
      schema: climate
  prod:
    mode: production
    workspace:
      host: https://adb-7405607848397610.10.azuredatabricks.net
      root_path: /Workspace/Users/randall.suarez@ibmconsultinghakkoda.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: commonsilver
      schema: climate
    permissions:
      - user_name: randall.suarez@ibmconsultinghakkoda.onmicrosoft.com
        level: CAN_MANAGE

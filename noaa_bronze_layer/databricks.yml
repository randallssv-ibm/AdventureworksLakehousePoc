# This is a Databricks asset bundle definition for noaa_bronze_layer.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: noaa_bronze_layer
  uuid: 876f6147-b225-4dd3-a8a2-2ff75589ea8c

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405616585742809.9.azuredatabricks.net
    variables:
      catalog: dev_bronze
      schema: stg_noaa
  prod:
    mode: production
    workspace:
      host: https://adb-7405616585742809.9.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/randallsuarez@adventureworkshkd.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Users/randallsuarez@adventureworkshkd.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dev_bronze
      schema: stg_noaa
    permissions:
      - user_name: randallsuarez@adventureworkshkd.onmicrosoft.com
        level: CAN_MANAGE

# This is a Databricks asset bundle definition for aw_silver_layer_models.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: aw_silver_layer_models
  uuid: 301b0e25-7497-434a-837f-fba12dfa4a05

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405616585742809.9.azuredatabricks.net
    variables:
      catalog: dev_silver
      schema: sales
  prod:
    mode: production
    workspace:
      host: https://adb-7405616585742809.9.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/randallsuarez@adventureworkshkd.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Users/randallsuarez@adventureworkshkd.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dev_silver
      schema: sales
    permissions:
      - user_name: randallsuarez@adventureworkshkd.onmicrosoft.com
        level: CAN_MANAGE
